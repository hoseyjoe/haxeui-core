package haxe.ui.dragdrop;

typedef DropOptions = {
    /**
     * A style name added to a drop target while a drag it accepts is over it
     * Default: drop-hover
     */
    @:optional var dropHoverStyleName:String;
}
